#include <iostream>
using namespace std;

enum class Lights { Red, Yellow, Green};
enum class Colors {Red, Yellow, Green};

int main() {
    Colors light = Colors::Red;
    Lights color = Lights::Green;
}